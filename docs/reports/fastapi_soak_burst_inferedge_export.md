# FastAPI Soak/Burst InferEdge Export Report

> Pending export. Execute `bash scripts/export_fastapi_soak_burst_inferedge.sh` after generating FastAPI soak/burst JSON evidence.

This report is generated by the exporter so that the Markdown summary matches the committed `metadata.json` / `result.json` handoff pair.
