# FastAPI ResNet18 Soak/Burst Report

> Pending Jetson run. Execute `bash scripts/run_fastapi_soak_burst.sh` to generate the current report and JSON evidence.

This report is intentionally generated by the benchmark runner so that the committed Markdown matches the measured JSON artifact.
